(
    shader_path: "shaders/sinusoids.wgsl",
    fps_ac: 60,
    fps_battery: 30,
    audio_sensitivity: 1.1,
    // Override the visualization per machine: `stripes` or `composite`.
    // visualization: composite,
)
